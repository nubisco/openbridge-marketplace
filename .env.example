# PostgreSQL connection string
DATABASE_URL=postgres://marketplace:password@localhost:5432/marketplace

# Secret for signing JWT session tokens (min 32 chars)
JWT_SECRET=change_me_to_a_long_random_string

# Postgres password (used by docker-compose)
POSTGRES_PASSWORD=change_me

# Cron schedule for the npm crawler (default: every 6 hours)
CRAWLER_SCHEDULE=0 */6 * * *

# Analytics collect endpoint — baked into the frontend at Docker build time.
# The NAS docker-compose does NOT need this; it is set in the CI workflow.
# Only needed if building the image locally.
VITE_ANALYTICS_ENDPOINT=https://analytics.nubisco.io/collect
